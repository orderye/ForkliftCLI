name: forklift_bao
description: ForkliftCLI — 智能叉车维修辅助工具
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 网络
  dio: ^5.4.0

  # 状态管理
  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0

  # 路由
  go_router: ^14.0.0

  # UI
  cupertino_icons: ^1.0.6
  cached_network_image: ^3.3.0
  flutter_svg: ^2.0.9

  # 本地存储
  shared_preferences: ^2.2.0

  # 相机/图片
  camera: ^0.11.0
  image_picker: ^1.0.0

  # 权限
  permission_handler: ^11.0.0

  # 聊天界面
  flutter_chat_ui: ^1.6.0
  flutter_chat_types: ^3.6.0

  # 3D/AR —— 全部走 Web 渲染器（webview_flutter + assets/vendor/ 内的 Three.js / model-viewer）
  # model_viewer_plus 已删除：lib 下 0 处引用，是 Unity 时代遗留的死依赖。
  # flutter_unity_widget: ^2022.2.1  # DEPRECATED: 已迁移到 Web 渲染器，如需回退请取消注释
  webview_flutter: ^4.8.0

  # 工具
  intl: ^0.19.0
  json_annotation: ^4.8.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
  riverpod_generator: ^2.3.0
  json_serializable: ^6.7.0

flutter:
  uses-material-design: true
  assets:
    - assets/
